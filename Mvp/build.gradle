apply plugin: 'com.android.library'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode rootProject.ext.versionCode
        versionName rootProject.ext.versionName

    }

    buildTypes {

    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    testImplementation rootProject.libraries.junit
    implementation rootProject.libraries.rxlifecycle
    implementation rootProject.libraries.rxlifecycle_android
    implementation rootProject.libraries.rxlifecycle_components
    implementation rootProject.libraries.rxjava2
    implementation rootProject.libraries.butterknife

    implementation rootProject.libraries.rxpermissions2


    androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.2'
}
